name: user_list_base_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  user_list_micro_core:
    git:
      url: https://github.com/EduFranchi/user_list_micro_core
      ref: main

  user_list_micro_app_user_list:
    git:
      url: https://github.com/EduFranchi/user_list_micro_app_user_list
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true